# brace-expansion@4.0.1 is a transitive of wireit (dev dependency).
# Fixed in 5.0.5 but the upgrade is a breaking change that wireit's
# minimatch chain does not support yet (no default export). This is a
# build-tool path with no runtime exposure to user input — DoS via
# pathological glob is not reachable from SGD's CLI surface.
[[IgnoredVulns]]
id = "GHSA-f886-m6hf-6m8v"
reason = "wireit dev dependency; not reachable from runtime; awaiting upstream fix"
