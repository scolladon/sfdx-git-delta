# --source-dir test already keeps expected/ out of the diff; this line stays deliberately so --ignore-file is still exercised against a real, populated directory
/expected
test/ignore/classes/**
test/ignore/labels/**