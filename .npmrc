; Every dependency is pinned exactly, runtime and dev alike: a range here becomes
; non-determinism for consumers and drift between the four sibling repositories.
; Let Dependabot move the pins. There is no lint enforcing this - npm outdated
; cannot see a range that still resolves to latest - so this file is the control.
save-exact=true
